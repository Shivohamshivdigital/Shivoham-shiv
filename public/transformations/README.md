# Transformation photos

Upload the before/after images here with these exact file names so they show
on the Weight-Loss page ("Real Transformations" section):

- `transformation-1.jpg`  → "Day 1 → Day 90"
- `transformation-2.jpg`  → "62 kg → 48 kg"
- `transformation-3.jpg`  → "Real client transformation"

Tip: keep each image under ~500 KB for fast loading. JPG or WEBP is fine
(if you use a different extension, update the `src` paths in
`src/views/WeightLossView.tsx`).
